# 🔒 LSB Стеганографія

Інтерактивний інструмент для приховування та витягування повідомлень із зображень методом найменш значущого біта (Least Significant Bit).

## ✨ Можливості

- **Приховування повідомлень** — вбудовування тексту в зображення без видимих змін
- **Витягування повідомлень** — відновлення прихованого тексту
- **Шифрування** — опціональний захист паролем (XOR з SHA-256)
- **Аналіз** — детальний звіт про зміни в зображенні
- **Візуалізація** — створення карти відмінностей

## 🚀 Встановлення

```bash
pip install -r requirements.txt
```

Або встановіть залежності вручну:

```bash
pip install Pillow rich
```

## 📖 Використання

### Інтерактивний режим

```bash
python main.py
```

### Як бібліотека

```python
from src import hide_message, extract_message, analyze_images

# Приховати повідомлення
report = hide_message(
    "original.png",
    "encoded.png",
    "Секретне повідомлення",
    password="my_password",
    bits_per_channel=1,
)
print(f"Використано {report.utilization * 100:.1f}% ємності")

# Витягнути повідомлення
message = extract_message(
    "encoded.png",
    password="my_password",
    bits_per_channel=1,
)
print(message)

# Аналіз відмінностей
diff = analyze_images("original.png", "encoded.png")
print(f"Змінено {diff.changed_channels} каналів")
```

## 🏗️ Структура проекту

```
lsb_stego/
├── main.py              # CLI інтерфейс
├── pyproject.toml       # Конфігурація проекту
├── README.md
└── src/
    ├── __init__.py      # Публічний API
    ├── bits.py          # Робота з бітовими потоками
    ├── crypto.py        # Криптографічні утиліти
    ├── models.py        # Моделі даних
    ├── prompts.py       # Інтерактивні запити
    ├── steganography.py # Основна логіка LSB
    └── ui.py            # Візуальні компоненти
```

## 🔧 Параметри

| Параметр | Опис | Значення |
|----------|------|----------|
| `bits_per_channel` | Кількість молодших бітів | 1-2 |
| `password` | Пароль для шифрування | рядок або None |
| `amplify` | Підсилення візуалізації | 1-255 |

## 📊 Звіти

### EmbedReport
- `payload_bits` — розмір повідомлення в бітах
- `capacity_bits` — загальна ємність
- `utilization` — відсоток використання
- `pixels_touched` — кількість змінених пікселів

### DiffReport
- `changed_channels` — кількість змінених каналів
- `avg_abs_diff` — середня абсолютна різниця
- `max_abs_diff` — максимальна різниця

## 🛡️ Безпека

- Шифрування використовує SHA-256 для генерації потоку ключів
- XOR шифрування забезпечує базовий захист
- Для критичних даних рекомендується додаткове шифрування

## 📝 Ліцензія

MIT License
