# 🛡️ SQL Injection Demo

Цей Flask-додаток створено для освітніх цілей. Він наочно демонструє різницю між вразливим та безпечним підходами до роботи з базами даних.

### Що демонструє проект:

| Сторінка | Метод | Результат |
|----------|-------|-----------|
| `/vulnerable` | Конкатенація рядків | ⚠️ SQL-ін'єкція можлива |
| `/secure` | Параметризовані запити | ✅ Ін'єкція заблокована |

## 🚀 Швидкий старт

### Вимоги

- Python 3.10+
- Flask

### Встановлення

```bash
# Встановіть залежності
pip install -r requirements.txt

# Запустіть додаток
python app.py
```

Додаток буде доступний за адресою: **http://localhost:5000**

## 📁 Структура проекту

```
lab06/
├── app.py                 # Основний Flask-додаток
├── lab.db                 # SQLite база даних (створюється автоматично)
├── README.md              # Документація
└── templates/
    ├── base.html          # Базовий шаблон
    ├── home.html          # Головна сторінка
    └── search.html        # Сторінка пошуку
```

## 🎯 Як користуватися

### Крок 1: Відкрийте головну сторінку

Перейдіть на http://localhost:5000 та оберіть режим демонстрації.

### Крок 2: Спробуйте тестовий payload

Введіть у поле пошуку:

```
' OR '1'='1
```

### Крок 3: Порівняйте результати

| Вразливий пошук | Безпечний пошук |
|-----------------|-----------------|
| Повертає **всі** записи БД | Повертає лише співпадіння |
| Payload виконується як SQL | Payload — просто текст |

## 🔍 Технічні деталі

### Вразливий запит (НЕ РОБІТЬ ТАК!)

```python
# ⚠️ НЕБЕЗПЕЧНО: пряма підстановка
sql = f"SELECT * FROM users WHERE name LIKE '%{query}%'"
```

При введенні `' OR '1'='1` запит стає:

```sql
SELECT * FROM users WHERE name LIKE '%' OR '1'='1%'
```

Умова `'1'='1'` завжди істинна → повертаються всі записи.

### Безпечний запит (ПРАВИЛЬНО)

```python
# ✅ БЕЗПЕЧНО: параметризований запит
sql = "SELECT * FROM users WHERE name LIKE ?"
cursor.execute(sql, (f"%{query}%",))
```

Параметр передається окремо від SQL-коду, тому ін'єкція неможлива.

## 🛠️ Особливості коду

### Архітектурні рішення

- **Dataclass** для конфігурації студента
- **Context manager** для з'єднань з БД
- **Type hints** для кращої читабельності
- **Модульна структура** з розділенням відповідальностей

### Дизайн інтерфейсу

- Сучасний glassmorphism UI
- Адаптивна верстка (mobile-first)
- Анімації та hover-ефекти
- Кольорове кодування безпеки (червоний/зелений)

## 📚 Корисні ресурси

### Про SQL-ін'єкції

- [OWASP SQL Injection](https://owasp.org/www-community/attacks/SQL_Injection)
- [SQL Injection Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html)

### Інструменти для тестування

- [SQLMap](https://sqlmap.org/) — автоматизоване тестування
- [Burp Suite](https://portswigger.net/burp) — проксі для аналізу

## ⚠️ Застереження

> **УВАГА:** Цей код містить навмисно вразливі компоненти виключно для освітніх цілей.
> 
> **НІКОЛИ** не використовуйте конкатенацію рядків для побудови SQL-запитів у реальних проектах!

## 📄 Ліцензія

Проект створено для освітніх цілей. Вільне використання в навчальному процесі.
